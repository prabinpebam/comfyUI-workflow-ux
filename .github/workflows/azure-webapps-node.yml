name: Deploy Node.js to Azure Web App

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

env:
  AZURE_WEBAPP_NAME: 'ComfyUI-workflow'    # From your parameters.json
  AZURE_WEBAPP_PACKAGE_PATH: '.'      
  NODE_VERSION: '20.x'                # Matches your linuxFxVersion in parameters.json
  COSMOS_DB_NAME: 'comfyui-workflow-server' # From your parameters.json

permissions:
  id-token: write  # Required for OIDC
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'

    - name: npm install and build
      run: |
        npm install
        npm run build

    - name: Upload artifact for deployment job
      uses: actions/upload-artifact@v4
      with:
        name: node-app
        path: |
          .
          !node_modules/**
          !.git/**

  deploy:
    permissions:
      id-token: write  # Required for OIDC
      contents: read
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Download artifact from build job
      uses: actions/download-artifact@v4
      with:
        name: node-app

    - name: Login to Azure using OIDC
      uses: azure/login@v1
      with:
        client-id: 'e6a68635-3fff-4129-8cad-778636d18143'      # From managed identity
        tenant-id: 'a4190f7e-d2ed-4eba-a23b-f436c5195d42'      # From managed identity
        subscription-id: '0675b2b4-5a9e-41bc-8f24-7d6d09a0ce48' # From parameters.json

    - name: Get MongoDB Connection String
      run: |
        CONNECTION_STRING=$(az cosmosdb keys list --type connection-strings --name ${{ env.COSMOS_DB_NAME }} --resource-group Web-apps --query "connectionStrings[0].connectionString" -o tsv)
        echo "MONGODB_URI=$CONNECTION_STRING" >> $GITHUB_ENV

    - name: 'Deploy to Azure WebApp'
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: ${{ env.AZURE_WEBAPP_NAME }}
        package: ${{ env.AZURE_WEBAPP_PACKAGE_PATH }}
        app-settings: |
          MONGODB_URI=${{ env.MONGODB_URI }}